{
  "name": "HipHop Watches",
  "version": "1.0.0",
  "author": "Waga",
  "homepage": "http://www.waga.it/",
  "repository": {
    "type": "git",
    "url": "https://github.com/wagaweb/waboot"
  },
  "scripts": {
    "wagabox:build": "docker exec wagabox-2.0.0 bash -c \"cd /var/www/html/waga/waboot/wp-content/themes/waboot && npm run composer:update\" && npm run assets:build",
    "composer:install": "composer install",
    "composer:update": "composer install && composer update",
    "build": "npm run composer:update && npm run assets:build",
    "assets:build": "npx gulp compile_js && npx gulp compile_css",
    "assets:build:watch": "npx gulp",
    "catalog:build:dev": "cd addons/packages/catalog/assets && npm i && npm run dev",
    "catalog:build:prod": "cd addons/packages/catalog/assets && npm i && npm run prod",
    "checkout:build:dev": "cd addons/packages/checkout/assets && npm i && npm run build-dev",
    "checkout:build:watch": "cd addons/packages/checkout/assets && npm i && npm run build-dev:watch",
    "checkout:build:prod": "cd addons/packages/checkout/assets && npm i && npm run build"
  },
  "license": "GPL-2.0+",
  "engines": {
    "node": ">= 4.4.3"
  },
  "devDependencies": {
    "@babel/compat-data": "^7.26.8",
    "@babel/core": "^7.26.10",
    "@babel/preset-env": "^7.26.9",
    "autoprefixer": "^10.4.21",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "browserify-shim": "^3.8.16",
    "cssnano": "^7.0.6",
    "gulp": "^5.0.0",
    "gulp-cli": "^2.3.0",
    "gulp-copy": "^5.0.0",
    "gulp-postcss": "^10.0.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^6.0.1",
    "gulp-sourcemaps": "^3.0.0",
    "gulp-terser": "^2.1.0",
    "gulp-uglify": "^3.0.2",
    "merge-stream": "^2.0.0",
    "sass": "^1.86.3",
    "vinyl-buffer": "^1.0.1",
    "vinyl-source-stream": "^2.0.0"
  },
  "browserify": {
    "transform": [
      "browserify-shim"
    ]
  },
  "browserify-shim": {
    "jquery": "global:jQuery",
    "backbone": "global:Backbone",
    "underscore": "global:_"
  },
  "browserslist": [
    "defaults and fully supports es6-module",
    "maintained node versions"
  ]
}