!function (e, t) {
    "use strict";
    var i = function () {
        function e(e, t, i, a) {
            return "string" == typeof e && "function" == typeof t && (i = parseInt(i || 10, 10), l("actions", e, t, i, a)), d
        }

        function t() {
            var e = Array.prototype.slice.call(arguments), t = e.shift();
            return "string" == typeof t && c("actions", t, e), d
        }

        function i(e, t) {
            return "string" == typeof e && o("actions", e, t), d
        }

        function a(e, t, i, a) {
            return "string" == typeof e && "function" == typeof t && (i = parseInt(i || 10, 10), l("filters", e, t, i)), d
        }

        function n() {
            var e = Array.prototype.slice.call(arguments), t = e.shift();
            return "string" == typeof t ? c("filters", t, e) : d
        }

        function s(e, t) {
            return "string" == typeof e && o("filters", e, t), d
        }

        function o(e, t, i, a) {
            if (f[e][t])if (i) {
                var n = f[e][t], s;
                if (a)for (s = n.length; s--;) {
                    var o = n[s];
                    o.callback === i && o.context === a && n.splice(s, 1)
                } else for (s = n.length; s--;)n[s].callback === i && n.splice(s, 1)
            } else f[e][t] = []
        }

        function l(e, t, i, a, n) {
            var s = {callback: i, priority: a, context: n}, o = f[e][t];
            o ? (o.push(s), o = r(o)) : o = [s], f[e][t] = o
        }

        function r(e) {
            for (var t, i, a, n = 1, s = e.length; s > n; n++) {
                for (t = e[n], i = n; (a = e[i - 1]) && a.priority > t.priority;)e[i] = e[i - 1], --i;
                e[i] = t
            }
            return e
        }

        function c(e, t, i) {
            var a = f[e][t];
            if (!a)return "filters" === e ? i[0] : !1;
            var n = 0, s = a.length;
            if ("filters" === e)for (; s > n; n++)i[0] = a[n].callback.apply(a[n].context, i); else for (; s > n; n++)a[n].callback.apply(a[n].context, i);
            return "filters" === e ? i[0] : !0
        }

        var d = {
            removeFilter: s,
            applyFilters: n,
            addFilter: a,
            removeAction: i,
            doAction: t,
            addAction: e
        }, f = {actions: {}, filters: {}};
        return d
    };
    e.wp = e.wp || {}, e.wp.hooks = new i
}(window);
var acf;
!function (e) {
    e.fn.exists = function () {
        return e(this).length > 0
    }, e.fn.outerHTML = function () {
        return e(this).get(0).outerHTML
    }, acf = {
        l10n: {}, o: {}, update: function (e, t) {
            this.o[e] = t
        }, get: function (e) {
            return "undefined" != typeof this.o[e] ? this.o[e] : null
        }, _e: function (e, t) {
            t = t || !1;
            var i = this.l10n[e] || "";
            return t && (i = i[t] || ""), i
        }, add_action: function () {
            var e = arguments[0].split(" ");
            for (k in e)arguments[0] = "acf." + e[k], wp.hooks.addAction.apply(this, arguments);
            return this
        }, remove_action: function () {
            return arguments[0] = "acf." + arguments[0], wp.hooks.removeAction.apply(this, arguments), this
        }, do_action: function () {
            return arguments[0] = "acf." + arguments[0], wp.hooks.doAction.apply(this, arguments), this
        }, add_filter: function () {
            return arguments[0] = "acf." + arguments[0], wp.hooks.addFilter.apply(this, arguments), this
        }, remove_filter: function () {
            return arguments[0] = "acf." + arguments[0], wp.hooks.removeFilter.apply(this, arguments), this
        }, apply_filters: function () {
            return arguments[0] = "acf." + arguments[0], wp.hooks.applyFilters.apply(this, arguments)
        }, get_fields: function (t, i, a) {
            t = t || {}, i = i || e("body"), a = a || !1;
            var n = ".acf-field";
            for (k in t)n += "[data-" + k + '="' + t[k] + '"]';
            var s = i.find(n);
            return i.is(n) && (s = s.add(i)), a || (s = s.filter(function () {
                return acf.apply_filters("is_field_ready_for_js", !0, e(this))
            })), s
        }, get_field: function (t, i) {
            i = i || e("body");
            var a = this.get_fields({key: t}, i, !0);
            return a.exists() ? a.first() : !1
        }, get_closest_field: function (e, t) {
            t = t || {};
            var i = ".acf-field";
            for (k in t)i += "[data-" + k + '="' + t[k] + '"]';
            return e.closest(i)
        }, get_field_wrap: function (e) {
            return e.closest(".acf-field")
        }, get_field_key: function (e) {
            return this.get_data(e, "key")
        }, get_field_type: function (e) {
            return this.get_data(e, "type")
        }, get_data: function (t, i) {
            i = i || !1;
            var a = this, n = !1;
            return i ? (n = t.attr("data-" + i), e.isNumeric(n) && (n.match(/[^0-9]/) || (n = parseInt(n)))) : (n = {}, e.each(t[0].attributes, function (e, s) {
                "data-" === s.name.substr(0, 5) && (i = s.name.replace("data-", ""), n[i] = a.get_data(t, i))
            })), n
        }, is_field: function (t, i) {
            if (i = i || {}, !t.hasClass("acf-field"))return !1;
            var a = !0;
            return e.each(i, function (e, i) {
                t.attr("data-" + e) != i && (a = !1)
            }), a
        }, is_sub_field: function (e, t) {
            t = t || !1;
            var i = !1;
            return $parent = e.parent().closest(".acf-field"), $parent.exists() && (i = !0, t && (i = this.is_field($parent, t))), i
        }, get_uniqid: function (e, t) {
            "undefined" == typeof e && (e = "");
            var i, a = function (e, t) {
                return e = parseInt(e, 10).toString(16), t < e.length ? e.slice(e.length - t) : t > e.length ? Array(1 + (t - e.length)).join("0") + e : e
            };
            return this.php_js || (this.php_js = {}), this.php_js.uniqidSeed || (this.php_js.uniqidSeed = Math.floor(123456789 * Math.random())), this.php_js.uniqidSeed++, i = e, i += a(parseInt((new Date).getTime() / 1e3, 10), 8), i += a(this.php_js.uniqidSeed, 5), t && (i += (10 * Math.random()).toFixed(8).toString()), i
        }, serialize_form: function (t, i) {
            i = i || "";
            var a = {}, n = {}, s = i.length, o = "_" + i, l = o.length;
            return $selector = t.find("select, textarea, input"), $selector = $selector.filter(function () {
                return e(this).closest(".postbox.acf-hidden").exists() ? !1 : !0
            }), e.each($selector.serializeArray(), function (e, t) {
                i && t.name.substring(0, s) != i && t.name.substring(0, l) != o || ("[]" === t.name.slice(-2) && (t.name = t.name.replace("[]", ""), "undefined" == typeof n[t.name] && (n[t.name] = -1), n[t.name]++, t.name += "[" + n[t.name] + "]"), a[t.name] = t.value)
            }), a
        }, remove_tr: function (e, t) {
            var i = e.height(), a = e.children().length;
            e.addClass("acf-remove-element"), setTimeout(function () {
                e.removeClass("acf-remove-element"), e.html('<td style="padding:0; height:' + i + 'px" colspan="' + a + '"></td>'), e.children("td").animate({height: 0}, 250, function () {
                    e.remove(), "function" == typeof t && t()
                })
            }, 250)
        }, remove_el: function (t, i, a) {
            a = a || 0, t.css({
                height: t.height(),
                width: t.width(),
                position: "absolute",
                padding: 0
            }), t.wrap('<div class="acf-temp-wrap" style="height:' + t.outerHeight(!0) + 'px"></div>'), t.animate({opacity: 0}, 250), t.parent(".acf-temp-wrap").animate({height: a}, 250, function () {
                e(this).remove(), "function" == typeof i && i()
            })
        }, isset: function () {
            var e = arguments, t = e.length, a = null, n;
            if (0 === t)throw new Error("Empty isset");
            for (a = e[0], i = 1; t > i; i++) {
                if (e[i] === n || a[e[i]] === n)return !1;
                a = a[e[i]]
            }
            return !0
        }, open_popup: function (t) {
            if ($popup = e("body > #acf-popup"), $popup.exists())return update_popup(t);
            var i = ['<div id="acf-popup">', '<div class="acf-popup-box acf-box">', '<div class="title"><h3></h3><a href="#" class="acf-icon acf-close-popup"><i class="acf-sprite-delete "></i></a></div>', '<div class="inner"></div>', '<div class="loading"><i class="acf-loading"></i></div>', "</div>", '<div class="bg"></div>', "</div>"].join("");
            return e("body").append(i), e("#acf-popup").on("click", ".bg, .acf-close-popup", function (e) {
                e.preventDefault(), acf.close_popup()
            }), this.update_popup(t)
        }, update_popup: function (t) {
            return $popup = e("#acf-popup"), $popup.exists() ? (t = e.extend({}, {
                title: "",
                content: "",
                width: 0,
                height: 0,
                loading: !1
            }, t), t.width && $popup.find(".acf-popup-box").css({
                width: t.width,
                "margin-left": 0 - t.width / 2
            }), t.height && $popup.find(".acf-popup-box").css({
                height: t.height,
                "margin-top": 0 - t.height / 2
            }), t.title && $popup.find(".title h3").html(t.title), t.content && $popup.find(".inner").html(t.content), t.loading ? $popup.find(".loading").show() : $popup.find(".loading").hide(), $popup) : !1
        }, close_popup: function () {
            $popup = e("#acf-popup"), $popup.exists() && $popup.remove()
        }, update_user_setting: function (t, i) {
            e.ajax({
                url: acf.get("ajaxurl"),
                dataType: "html",
                type: "post",
                data: acf.prepare_for_ajax({action: "acf/update_user_setting", name: t, value: i})
            })
        }, prepare_for_ajax: function (e) {
            return e.nonce = acf.get("nonce"), e = acf.apply_filters("prepare_for_ajax", e)
        }, is_ajax_success: function (e) {
            return e && e.success ? !0 : !1
        }, update_cookie: function (e, t, i) {
            if (i = i || 31) {
                var a = new Date;
                a.setTime(a.getTime() + 24 * i * 60 * 60 * 1e3);
                var n = "; expires=" + a.toGMTString()
            } else var n = "";
            document.cookie = e + "=" + t + n + "; path=/"
        }, get_cookie: function (e) {
            for (var t = e + "=", i = document.cookie.split(";"), a = 0; a < i.length; a++) {
                for (var n = i[a]; " " == n.charAt(0);)n = n.substring(1, n.length);
                if (0 == n.indexOf(t))return n.substring(t.length, n.length)
            }
            return null
        }, delete_cookie: function (e) {
            this.update_cookie(e, "", -1)
        }, is_in_view: function (t) {
            var i = e(window).scrollTop(), a = i + e(window).height(), n = t.offset().top, s = n + t.height();
            return a >= s && n >= i
        }
    }, acf.model = {
        actions: {}, filters: {}, events: {}, extend: function (t) {
            var i = e.extend({}, this, t);
            return e.each(i.actions, function (e, t) {
                var a = e.split(" "), e = a[0] || "", n = a[1] || 10;
                acf.add_action(e, i[t], n, i)
            }), e.each(i.filters, function (e, t) {
                var a = e.split(" "), e = a[0] || "", n = a[1] || 10;
                acf.add_filter(e, i[t], n, i)
            }), e.each(i.events, function (t, a) {
                var n = t.substr(0, t.indexOf(" ")), s = t.substr(t.indexOf(" ") + 1);
                e(document).on(n, s, function (t) {
                    t.$el = e(this), i[a].apply(i, [t])
                })
            }), i
        }
    }, acf.media = acf.model.extend({
        actions: {ready: "onReady", load: "onLoad"}, popup: function (t) {
            var i = {mode: "select", title: "", button: "", type: "", library: "all", multiple: !1};
            t = e.extend({}, i, t);
            var a = {title: t.title, multiple: t.multiple, library: {}, states: []};
            t.type && (a.library = {type: t.type}), "edit" == t.mode && (a.library = {post__in: [t.id]}), t.button && (a.button = {text: t.button}), a.states = [new wp.media.controller.Library({
                library: wp.media.query(a.library),
                multiple: a.multiple,
                title: a.title,
                priority: 20,
                filterable: "all",
                editable: !0,
                allowLocalEdits: !0
            })], "undefined" != typeof wp.media.controller.EditImage && a.states.push(new wp.media.controller.EditImage);
            var n = wp.media(a);
            return n.on("content:render:edit-image", function () {
                var e = this.state().get("image"), t = new wp.media.view.EditImage({
                    model: e,
                    controller: this
                }).render();
                this.content.set(t), t.loadEditor()
            }, n), n.on("content:activate:browse", function () {
                try {
                    var i = n.content.get(), a = i.toolbar, s = a.get("filters")
                } catch (o) {
                    return
                }
                "uploadedTo" == t.library && e.isNumeric(acf.get("post_id")) && (s.$el.find('option[value="uploaded"]').remove(), s.$el.after('<span class="acf-uploadedTo">' + acf._e("image", "uploadedTo") + "</span>"), e.each(s.filters, function (e, t) {
                    t.props.uploadedTo = acf.get("post_id")
                })), "image" == t.type && (e.each(s.filters, function (e, t) {
                    t.props.type = "image"
                }), s.$el.find("option").each(function () {
                    var i = e(this).attr("value");
                    ("uploaded" != i || "all" != t.library) && -1 === i.indexOf("image") && e(this).remove()
                }), s.$el.val("image")), s.$el.trigger("change")
            }), "function" == typeof t.select && n.on("select", function () {
                var e = this, i = -1, a = n.state().get("selection");
                a && a.each(function (a) {
                    i++, t.select.apply(e, [a, i])
                })
            }), n.on("close", function () {
                setTimeout(function () {
                    n.detach(), n.dispose(), n = null
                }, 500)
            }), "edit" == t.mode && (n.on("open", function () {
                "browse" != this.content.mode() && this.content.mode("browse"), this.$el.closest(".media-modal").addClass("acf-media-modal acf-expanded");
                var e = this.state(), i = e.get("selection"), a = wp.media.attachment(t.id);
                i.add(a)
            }, n), n.on("close", function () {
                n.$el.closest(".media-modal").removeClass("acf-media-modal")
            })), t.button && n.on("toolbar:create:select", function (e) {
                a = {text: t.button, controller: this}, e.view = new wp.media.view.Toolbar.Select(a)
            }, n), setTimeout(function () {
                n.open()
            }, 1), n
        }, onReady: function () {
            var t = acf.get("wp_version");
            t && e("body").addClass("acf-wp-" + t.substr(0, 1))
        }, onLoad: function () {
            if ("undefined" == typeof wp)return !1;
            if (!acf.isset(wp, "media", "view", "AttachmentCompat", "prototype"))return !1;
            var t = wp.media.view.AttachmentCompat.prototype;
            t.orig_render = t.render, t.orig_dispose = t.dispose, t.render = function () {
                var t = this;
                return t.ignore_render ? this : (this.orig_render(), setTimeout(function () {
                    var i = t.$el.closest(".media-modal");
                    if (!i.hasClass("acf-media-modal") && !i.find(".media-frame-router .acf-expand-details").exists()) {
                        var a = e(['<a href="#" class="acf-expand-details">', '<span class="is-closed"><span class="acf-icon small"><i class="acf-sprite-left"></i></span>' + acf._e("expand_details") + "</span>", '<span class="is-open"><span class="acf-icon small"><i class="acf-sprite-right"></i></span>' + acf._e("collapse_details") + "</span>", "</a>"].join(""));
                        a.on("click", function (e) {
                            e.preventDefault(), i.hasClass("acf-expanded") ? i.removeClass("acf-expanded") : i.addClass("acf-expanded")
                        }), i.find(".media-frame-router").append(a)
                    }
                }, 0), clearTimeout(acf.media.render_timout), acf.media.render_timout = setTimeout(function () {
                    acf.do_action("append", t.$el)
                }, 50), this)
            }, t.dispose = function () {
                acf.do_action("remove", this.$el), this.orig_dispose()
            }, t.save = function (e) {
                e && e.preventDefault();
                var t = acf.serialize_form(this.$el);
                this.ignore_render = !0, this.model.saveCompat(t)
            }, setTimeout(function () {
                try {
                    e.isNumeric(acf.o.post_id) && (wp.media.view.settings.post.id = acf.o.post_id)
                } catch (t) {
                }
            }, 10)
        }
    }), acf.conditional_logic = acf.model.extend({
        actions: {"ready 20": "render", "append 20": "render"},
        events: {
            "change .acf-field input": "change",
            "change .acf-field textarea": "change",
            "change .acf-field select": "change"
        },
        items: {},
        triggers: {},
        add: function (e, t) {
            var i = this;
            this.items[e] = t;
            for (var a in t) {
                var n = t[a];
                for (var s in n) {
                    var o = n[s];
                    "undefined" == typeof this.triggers[o.field] && (this.triggers[o.field] = []), -1 === this.triggers[o.field].indexOf(e) && this.triggers[o.field].push(e)
                }
            }
        },
        change: function (e) {
            var t = e.$el, i = acf.get_field_wrap(t), a = i.parent(), n = acf.get_field_key(i);
            if ("undefined" == typeof this.triggers[n])return !1;
            for (var s in this.triggers[n]) {
                var o = this.triggers[n][s], l = acf.get_fields({key: o}, a, !0);
                this.render_fields(l)
            }
        },
        render: function (t) {
            t = t || e("body");
            var i = acf.get_fields({}, t);
            this.render_fields(i)
        },
        render_fields: function (t) {
            var i = this;
            t.each(function () {
                i.render_field(e(this))
            })
        },
        render_field: function (e) {
            var t = this, i = !1, a = acf.get_field_key(e);
            if ("undefined" == typeof this.items[a])return !1;
            var n = this.items[a];
            for (var s in n) {
                var o = n[s], l = !0;
                for (var r in o) {
                    var c = o[r];
                    if (!t.get_visibility(e, c)) {
                        l = !1;
                        break
                    }
                }
                if (l) {
                    i = !0;
                    break
                }
            }
            i ? t.show_field(e) : t.hide_field(e)
        },
        show_field: function (e) {
            e.removeClass("hidden-by-conditional-logic"), e.find("input, textarea, select").not(".acf-disabled").removeAttr("disabled"), acf.do_action("conditional_logic_show_field", e), acf.do_action("show_field", e, "conditional_logic")
        },
        hide_field: function (e) {
            e.addClass("hidden-by-conditional-logic"), e.find("input, textarea, select").attr("disabled", "disabled"), acf.do_action("conditional_logic_hide_field", e), acf.do_action("hide_field", e, "conditional_logic")
        },
        get_visibility: function (t, i) {
            var a = acf.get_fields({key: i.field}, !1, !0), n = null;
            if (!a.exists())return !1;
            n = a.first(), a.length > 1 && a.each(function () {
                return $parent = e(this).parent(), t.closest($parent).exists() ? (n = e(this), !1) : void 0
            });
            var s = this.calculate(i, n, t);
            return s
        },
        calculate: function (t, i, a) {
            var n = acf.get_data(i, "type");
            if ("true_false" == n || "checkbox" == n || "radio" == n) {
                var s = i.find('input[value="' + t.value + '"]:checked').exists();
                if ("==" == t.operator && s)return !0;
                if ("!=" == t.operator && !s)return !0
            } else if ("select" == n) {
                var o = i.find("select"), l = acf.get_data(o), r = [];
                if (l.multiple && l.ui ? i.find(".acf-select2-multi-choice").each(function () {
                        r.push(e(this).val())
                    }) : l.multiple ? r = o.val() : r.push(l.ui ? i.find("input").first().val() : o.val()), "==" == t.operator) {
                    if (e.inArray(t.value, r) > -1)return !0
                } else if (e.inArray(t.value, r) < 0)return !0
            }
            return !1
        }
    }), e(document).ready(function () {
        acf.do_action("ready", e("body"))
    }), e(window).load(function () {
        acf.do_action("load", e("body"))
    }), e(document).on("click", '.acf-field a[href="#"]', function (e) {
        e.preventDefault()
    }), e(document).on("change", ".acf-field input, .acf-field textarea, .acf-field select", function () {
        e('#acf-form-data input[name="_acfchanged"]').exists() && e('#acf-form-data input[name="_acfchanged"]').val(1), acf.update("changed", !0)
    });
    var t = function () {
        return acf.get("changed") ? acf._e("unload") : void 0
    };
    acf.add_filter("validation_complete", function (i, a) {
        return i.errors && e(window).on("beforeunload", t), i
    }), e(document).on("submit", "form", function (i) {
        e(window).off("beforeunload", t)
    }), acf.add_action("submit", function (i) {
        e(window).off("beforeunload", t)
    }), e(window).on("beforeunload", t), acf.add_action("sortstart", function (t, i) {
        t.is("tr") && (t.css("position", "relative"), t.children().each(function () {
            e(this).width(e(this).width())
        }), t.css("position", "absolute"), i.html('<td style="height:' + t.height() + 'px; padding:0;" colspan="' + t.children("td").length + '"></td>'))
    }), acf.add_action("before_duplicate", function (t) {
        t.find("select").each(function () {
            e(this).find(":selected").addClass("selected")
        })
    }), acf.add_action("after_duplicate", function (t, i) {
        t.find("select").each(function () {
            e(this).find(".selected").removeClass("selected")
        }), i.find("select").each(function () {
            var t = e(this).find(".selected");
            e(this).val(t.attr("value")), t.removeClass("selected")
        })
    }), acf.add_action("ready", function (t) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("ready_field", e(this)), acf.do_action("ready_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("append", function (t) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("append_field", e(this)), acf.do_action("append_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("load", function (t) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("load_field", e(this)), acf.do_action("load_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("remove", function (t) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("remove_field", e(this)), acf.do_action("remove_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("sortstart", function (t, i) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("sortstart_field", e(this)), acf.do_action("sortstart_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("sortstop", function (t, i) {
        acf.get_fields({}, t).each(function () {
            acf.do_action("sortstop_field", e(this)), acf.do_action("sortstop_field/type=" + acf.get_field_type(e(this)), e(this))
        })
    }), acf.add_action("hide_field", function (e, t) {
        acf.do_action("hide_field/type=" + acf.get_field_type(e), e, t)
    }), acf.add_action("show_field", function (e, t) {
        acf.do_action("show_field/type=" + acf.get_field_type(e), e, t)
    }), acf.fields = {}, acf.field = {
        type: "",
        settings: {},
        actions: {},
        events: {},
        $field: null,
        extend: function (t) {
            var i = e.extend({}, this, t);
            return e.each(i.actions, function (e, t) {
                var e = e + "_field/type=" + i.type;
                acf.add_action(e, function () {
                    [].unshift.apply(arguments, [t]), i.doAction.apply(i, arguments)
                })
            }), e.each(i.events, function (t, a) {
                var n = t.substr(0, t.indexOf(" ")), s = t.substr(t.indexOf(" ") + 1);
                e(document).on(n, '.acf-field[data-type="' + i.type + '"] ' + s, function (t) {
                    t.$el = e(this), i.doEvent.apply(i, [a, t])
                })
            }), i
        },
        doFocus: function (t) {
            return this.$field = t, e.extend(this.settings, acf.get_data(t)), "function" == typeof this.focus && this.focus(), this
        },
        doAction: function () {
            var e = [].shift.apply(arguments);
            this.doFocus(arguments[0]), this[e].apply(this, arguments)
        },
        doEvent: function (e, t) {
            this.doFocus(acf.get_closest_field(t.$el, {type: this.type})), this[e].apply(this, [t])
        }
    }
}(jQuery), function (e) {
    acf.ajax = acf.model.extend({
        actions: {ready: "onReady"},
        o: {
            action: "acf/post/get_field_groups",
            post_id: 0,
            page_template: 0,
            page_parent: 0,
            page_type: 0,
            post_format: 0,
            post_taxonomy: 0,
            lang: 0
        },
        update: function (e, t) {
            return this.o[e] = t, this
        },
        get: function (e) {
            return this.o[e] || null
        },
        onReady: function () {
            if (!acf.get("ajax"))return !1;
            if (this.update("post_id", acf.get("post_id")), e("#icl-als-first").length > 0) {
                var t = e("#icl-als-first").children("a").attr("href"), i = new RegExp("lang=([^&#]*)"), a = i.exec(t);
                this.update("lang", a[1])
            }
            this.add_events()
        },
        fetch: function () {
            var t = this;
            e.ajax({
                url: acf.get("ajaxurl"),
                data: acf.prepare_for_ajax(this.o),
                type: "post",
                dataType: "json",
                success: function (e) {
                    acf.is_ajax_success(e) && t.render(e.data)
                }
            })
        },
        render: function (t) {
            e(".acf-postbox").addClass("acf-hidden"), e(".acf-postbox-toggle").addClass("acf-hidden"), e.each(t, function (t, i) {
                var a = e("#acf-" + i.key), n = e('#adv-settings .acf_postbox-toggle[for="acf-' + i.key + '-hide"]');
                a.removeClass("acf-hidden hide-if-js"), n.removeClass("acf-hidden hide-if-js"), n.find('input[type="checkbox"]').attr("checked", "checked"), a.find(".acf-replace-with-fields").each(function () {
                    e(this).replaceWith(i.html), acf.do_action("append", a)
                }), 0 === t && e("#acf-style").html(i.style)
            })
        },
        sync_taxonomy_terms: function () {
            var t = [];
            e(".categorychecklist, .acf-taxonomy-field").each(function () {
                var i = e(this), a = i.find('input[type="checkbox"]').not(":disabled"), n = i.find('input[type="radio"]').not(":disabled"), s = i.find("select").not(":disabled"), o = i.find('input[type="hidden"]').not(":disabled");
                i.is(".acf-taxonomy-field") && "1" != i.attr("data-load_save") || i.closest(".media-frame").exists() || (a.exists() ? a.filter(":checked").each(function () {
                    t.push(e(this).val())
                }) : n.exists() ? n.filter(":checked").each(function () {
                    t.push(e(this).val())
                }) : s.exists() ? s.find("option:selected").each(function () {
                    t.push(e(this).val())
                }) : o.exists() && o.each(function () {
                    !e(this).val() || e(this).val().indexOf(",") > -1 || t.push(e(this).val())
                }))
            }), t = t.filter(function (e, t, i) {
                return i.indexOf(e) == t
            }), this.update("post_taxonomy", t).fetch()
        },
        add_events: function () {
            var t = this;
            e(document).on("change", "#page_template", function () {
                var i = e(this).val();
                t.update("page_template", i).fetch()
            }), e(document).on("change", "#parent_id", function () {
                var i = "parent", a = 0;
                "" != e(this).val() && (i = "child", a = e(this).val()), t.update("page_type", i).update("page_parent", a).fetch()
            }), e(document).on("change", '#post-formats-select input[type="radio"]', function () {
                var i = e(this).val();
                "0" == i && (i = "standard"), t.update("post_format", i).fetch()
            }), e(document).on("change", ".categorychecklist input, .acf-taxonomy-field input, .acf-taxonomy-field select", function () {
                var i = e(this).closest(".acf-taxonomy-field");
                i.exists() && "1" != i.attr("data-load_save") || e(this).closest(".media-frame").exists() || setTimeout(function () {
                    t.sync_taxonomy_terms()
                }, 1)
            })
        }
    })
}(jQuery), function (e) {
    acf.fields.color_picker = acf.field.extend({
        type: "color_picker",
        timeout: null,
        actions: {ready: "initialize", append: "initialize"},
        focus: function () {
            this.$input = this.$field.find('input[type="text"]')
        },
        initialize: function () {
            var e = this, t = this.$input.clone();
            t.attr({
                type: "hidden",
                "class": "",
                id: "",
                value: ""
            }), this.$input.before(t), this.$input.wpColorPicker({
                change: function (i, a) {
                    e.timeout && clearTimeout(e.timeout), e.timeout = setTimeout(function () {
                        t.trigger("change")
                    }, 1e3)
                }
            })
        }
    })
}(jQuery), function (e) {
    acf.fields.date_picker = acf.field.extend({
        type: "date_picker",
        $el: null,
        $input: null,
        $hidden: null,
        actions: {ready: "initialize", append: "initialize"},
        events: {'blur input[type="text"]': "blur"},
        focus: function () {
            this.$el = this.$field.find(".acf-date_picker"), this.$input = this.$el.find('input[type="text"]'), this.$hidden = this.$el.find('input[type="hidden"]'), this.settings = acf.get_data(this.$el)
        },
        initialize: function () {
            this.$input.val(this.$hidden.val());
            var t = e.extend({}, acf.l10n.date_picker, {
                dateFormat: "yymmdd",
                altField: this.$hidden,
                altFormat: "yymmdd",
                changeYear: !0,
                yearRange: "-100:+100",
                changeMonth: !0,
                showButtonPanel: !0,
                firstDay: this.settings.first_day
            });
            t = acf.apply_filters("date_picker_args", t, this.$el), this.$input.addClass("active").datepicker(t), this.$input.datepicker("option", "dateFormat", this.settings.display_format), e("body > #ui-datepicker-div").exists() && e("body > #ui-datepicker-div").wrap('<div class="acf-ui-datepicker" />')
        },
        blur: function () {
            this.$input.val() || this.$hidden.val("")
        }
    })
}(jQuery), function (e) {
    acf.fields.file = acf.field.extend({
        type: "file",
        $el: null,
        events: {
            'click [data-name="add"]': "add",
            'click [data-name="edit"]': "edit",
            'click [data-name="remove"]': "remove"
        },
        focus: function () {
            this.$el = this.$field.find(".acf-file-uploader"), this.settings = acf.get_data(this.$el)
        },
        add: function () {
            var t = this, i = acf.get_data(this.$field, "key"), a = acf.get_closest_field(this.$field, {type: "repeater"}), n = acf.media.popup({
                title: acf._e("file", "select"),
                mode: "select",
                type: "",
                multiple: a.exists(),
                library: this.settings.library,
                select: function (n, s) {
                    if (s > 0) {
                        var o = t.$field.parent(), l = !1;
                        if (o.nextAll(".acf-row").not(".clone").each(function () {
                                return (l = acf.get_field(i, e(this))) ? l.find(".acf-file-uploader.has-value").exists() ? void(l = !1) : !1 : void 0
                            }), !l) {
                            if (o = acf.fields.repeater.doFocus(a).add(), !o)return !1;
                            l = acf.get_field(i, o)
                        }
                        t.doFocus(l)
                    }
                    t.render(t.prepare(n))
                }
            })
        },
        prepare: function (e) {
            var t = {
                id: e.id,
                title: e.attributes.title,
                name: e.attributes.filename,
                url: e.attributes.url,
                icon: e.attributes.icon,
                size: e.attributes.filesize
            };
            return t
        },
        render: function (e) {
            this.$el.find('[data-name="icon"]').attr("src", e.icon), this.$el.find('[data-name="title"]').text(e.title), this.$el.find('[data-name="name"]').text(e.name).attr("href", e.url), this.$el.find('[data-name="size"]').text(e.size), this.$el.find('[data-name="id"]').val(e.id).trigger("change"), this.$el.addClass("has-value")
        },
        edit: function () {
            var e = this, t = this.$el.find('[data-name="id"]').val(), i = acf.media.popup({
                title: acf._e("file", "edit"),
                button: acf._e("file", "update"),
                mode: "edit",
                id: t,
                select: function (t, i) {
                    e.render(e.prepare(t))
                }
            })
        },
        remove: function () {
            var e = {id: "", title: "", name: "", url: "", icon: "", size: ""};
            this.render(e), this.$el.removeClass("has-value")
        }
    })
}(jQuery), function (e) {
    acf.fields.google_map = {
        $el: null,
        $input: null,
        o: {},
        ready: !1,
        geocoder: !1,
        map: !1,
        maps: {},
        set: function (t) {
            return e.extend(this, t), this.$input = this.$el.find(".value"), this.o = acf.get_data(this.$el), this.maps[this.o.id] && (this.map = this.maps[this.o.id]), this
        },
        init: function () {
            this.geocoder || (this.geocoder = new google.maps.Geocoder), this.ready = !0, this.render()
        },
        render: function () {
            var e = this, t = this.$el, i = {
                zoom: parseInt(this.o.zoom),
                center: new google.maps.LatLng(this.o.lat, this.o.lng),
                mapTypeId: google.maps.MapTypeId.ROADMAP
            };
            this.map = new google.maps.Map(this.$el.find(".canvas")[0], i);
            var a = new google.maps.places.Autocomplete(this.$el.find(".search")[0]);
            a.map = this.map, a.bindTo("bounds", this.map), this.map.marker = new google.maps.Marker({
                draggable: !0,
                raiseOnDrag: !0,
                map: this.map
            }), this.map.$el = this.$el;
            var n = this.$el.find(".input-lat").val(), s = this.$el.find(".input-lng").val();
            n && s && this.update(n, s).center(), google.maps.event.addListener(a, "place_changed", function (t) {
                var i = this.map.$el, a = i.find(".search").val();
                i.find(".input-address").val(a), i.find(".title h4").text(a);
                var n = this.getPlace();
                if (n.geometry) {
                    var s = n.geometry.location.lat(), o = n.geometry.location.lng();
                    e.set({$el: i}).update(s, o).center()
                } else e.geocoder.geocode({address: a}, function (t, a) {
                    if (a != google.maps.GeocoderStatus.OK)return void console.log("Geocoder failed due to: " + a);
                    if (!t[0])return void console.log("No results found");
                    n = t[0];
                    var s = n.geometry.location.lat(), o = n.geometry.location.lng();
                    e.set({$el: i}).update(s, o).center()
                })
            }), google.maps.event.addListener(this.map.marker, "dragend", function () {
                var t = this.map.$el, i = this.map.marker.getPosition(), a = i.lat(), n = i.lng();
                e.set({$el: t}).update(a, n).sync()
            }), google.maps.event.addListener(this.map, "click", function (t) {
                var i = this.$el, a = t.latLng.lat(), n = t.latLng.lng();
                e.set({$el: i}).update(a, n).sync()
            }), this.maps[this.o.id] = this.map
        },
        update: function (e, t) {
            var i = new google.maps.LatLng(e, t);
            return this.$el.find(".input-lat").val(e), this.$el.find(".input-lng").val(t).trigger("change"), this.map.marker.setPosition(i), this.map.marker.setVisible(!0), this.$el.addClass("active"), this.$el.closest(".acf-field").removeClass("error"), this
        },
        center: function () {
            var e = this.map.marker.getPosition(), t = this.o.lat, i = this.o.lng;
            e && (t = e.lat(), i = e.lng());
            var a = new google.maps.LatLng(t, i);
            this.map.setCenter(a)
        },
        sync: function () {
            var e = this.$el, t = this.map.marker.getPosition(), i = new google.maps.LatLng(t.lat(), t.lng());
            return this.geocoder.geocode({latLng: i}, function (t, i) {
                if (i != google.maps.GeocoderStatus.OK)return void console.log("Geocoder failed due to: " + i);
                if (!t[0])return void console.log("No results found");
                var a = t[0];
                e.find(".title h4").text(a.formatted_address), e.find(".input-address").val(a.formatted_address).trigger("change")
            }), this
        },
        locate: function () {
            var e = this, t = this.$el;
            return navigator.geolocation ? (t.find(".title h4").text(acf.l10n.google_map.locating + "..."), t.addClass("active"), void navigator.geolocation.getCurrentPosition(function (i) {
                var a = i.coords.latitude, n = i.coords.longitude;
                e.set({$el: t}).update(a, n).sync().center()
            })) : (alert(acf.l10n.google_map.browser_support), this)
        },
        clear: function () {
            this.$el.removeClass("active"), this.$el.find(".search").val(""), this.$el.find(".input-address").val(""), this.$el.find(".input-lat").val(""), this.$el.find(".input-lng").val(""), this.map.marker.setVisible(!1)
        },
        edit: function () {
            this.$el.removeClass("active");
            var e = this.$el.find(".title h4").text();
            this.$el.find(".search").val(e).focus()
        },
        refresh: function () {
            google.maps.event.trigger(this.map, "resize"), this.center()
        }
    }, acf.add_action("ready append", function (t) {
        var i = acf.get_fields({type: "google_map"}, t);
        i.exists() && ("undefined" == typeof google ? e.getScript("https://www.google.com/jsapi", function () {
            google.load("maps", "3", {
                other_params: "sensor=false&libraries=places", callback: function () {
                    i.each(function () {
                        acf.fields.google_map.set({$el: e(this).find(".acf-google-map")}).init()
                    })
                }
            })
        }) : i.each(function () {
            acf.fields.google_map.set({$el: e(this).find(".acf-google-map")}).init()
        }))
    }), e(document).on("click", '.acf-google-map a[data-name="clear-location"]', function (t) {
        t.preventDefault(), acf.fields.google_map.set({$el: e(this).closest(".acf-google-map")}).clear(), e(this).blur()
    }), e(document).on("click", '.acf-google-map a[data-name="find-location"]', function (t) {
        t.preventDefault(), acf.fields.google_map.set({$el: e(this).closest(".acf-google-map")}).locate(), e(this).blur()
    }), e(document).on("click", ".acf-google-map .title h4", function (t) {
        t.preventDefault(), acf.fields.google_map.set({$el: e(this).closest(".acf-google-map")}).edit()
    }), e(document).on("keydown", ".acf-google-map .search", function (e) {
        return 13 == e.which ? !1 : void 0
    }), e(document).on("blur", ".acf-google-map .search", function (t) {
        var i = e(this).closest(".acf-google-map");
        i.find(".input-lat").val() && i.addClass("active")
    }), acf.add_action("show_field", function (e) {
        acf.fields.google_map.ready && acf.is_field(e, {type: "google_map"}) && acf.fields.google_map.set({$el: e.find(".acf-google-map")}).refresh()
    })
}(jQuery), function (e) {
    acf.fields.image = acf.field.extend({
        type: "image",
        $el: null,
        events: {
            'click [data-name="add"]': "add",
            'click [data-name="edit"]': "edit",
            'click [data-name="remove"]': "remove"
        },
        focus: function () {
            this.$el = this.$field.find(".acf-image-uploader"), this.settings = acf.get_data(this.$el)
        },
        add: function () {
            var t = this, i = acf.get_data(this.$field, "key"), a = acf.get_closest_field(this.$field, {type: "repeater"}), n = acf.media.popup({
                title: acf._e("image", "select"),
                mode: "select",
                type: "image",
                multiple: a.exists(),
                library: this.settings.library,
                select: function (n, s) {
                    if (s > 0) {
                        var o = t.$field.parent(), l = !1;
                        if (o.nextAll(".acf-row").not(".clone").each(function () {
                                return (l = acf.get_field(i, e(this))) ? l.find(".acf-image-uploader.has-value").exists() ? void(l = !1) : !1 : void 0
                            }), !l) {
                            if (o = acf.fields.repeater.doFocus(a).add(), !o)return !1;
                            l = acf.get_field(i, o)
                        }
                        t.doFocus(l)
                    }
                    t.render(t.prepare(n))
                }
            })
        },
        prepare: function (e) {
            var t = {id: e.id, url: e.attributes.url};
            return acf.isset(e.attributes, "sizes", this.settings.preview_size, "url") && (t.url = e.attributes.sizes[this.settings.preview_size].url), t
        },
        render: function (e) {
            this.$el.find('[data-name="image"]').attr("src", e.url), this.$el.find('[data-name="id"]').val(e.id).trigger("change"), this.$el.addClass("has-value")
        },
        edit: function () {
            var e = this, t = this.$el.find('[data-name="id"]').val(), i = acf.media.popup({
                title: acf._e("image", "edit"),
                button: acf._e("image", "update"),
                mode: "edit",
                id: t,
                select: function (t, i) {
                    e.render(e.prepare(t))
                }
            })
        },
        remove: function () {
            var e = {id: "", url: ""};
            this.render(e), this.$el.removeClass("has-value")
        }
    })
}(jQuery), function (e) {
    acf.fields.oembed = {
        search: function (t) {
            var i = t.find('[data-name="search-input"]').val();
            "http" != i.substr(0, 4) && (i = "http://" + i, t.find('[data-name="search-input"]').val(i)), t.addClass("is-loading");
            var a = {
                action: "acf/fields/oembed/search",
                nonce: acf.get("nonce"),
                s: i,
                width: acf.get_data(t, "width"),
                height: acf.get_data(t, "height")
            };
            t.data("xhr") && t.data("xhr").abort();
            var n = e.ajax({
                url: acf.get("ajaxurl"), data: a, type: "post", dataType: "html", success: function (e) {
                    t.removeClass("is-loading"), acf.fields.oembed.search_success(t, i, e), e || acf.fields.oembed.search_error(t)
                }
            });
            t.data("xhr", n)
        }, search_success: function (e, t, i) {
            e.removeClass("has-error").addClass("has-value"), e.find('[data-name="value-input"]').val(t), e.find('[data-name="value-title"]').html(t), e.find('[data-name="value-embed"]').html(i)
        }, search_error: function (e) {
            e.removeClass("has-value").addClass("has-error")
        }, clear: function (e) {
            e.removeClass("has-error has-value"), e.find('[data-name="search-input"]').val(""), e.find('[data-name="value-input"]').val(""), e.find('[data-name="value-title"]').html(""), e.find('[data-name="value-embed"]').html("")
        }, edit: function (e) {
            e.addClass("is-editing");
            var t = e.find('[data-name="value-title"]').text();
            e.find('[data-name="search-input"]').val(t).focus()
        }, blur: function (e) {
            e.removeClass("is-editing");
            var t = e.find('[data-name="value-title"]').text(), i = e.find('[data-name="search-input"]').val(), a = e.find('[data-name="value-embed"]').html();
            return i ? void(i != t && this.search(e)) : void this.clear(e)
        }
    }, e(document).on("click", '.acf-oembed [data-name="search-button"]', function (t) {
        t.preventDefault(), acf.fields.oembed.search(e(this).closest(".acf-oembed")), e(this).blur()
    }), e(document).on("click", '.acf-oembed [data-name="clear-button"]', function (t) {
        t.preventDefault(), acf.fields.oembed.clear(e(this).closest(".acf-oembed")), e(this).blur()
    }), e(document).on("click", '.acf-oembed [data-name="value-title"]', function (t) {
        t.preventDefault(), acf.fields.oembed.edit(e(this).closest(".acf-oembed"))
    }), e(document).on("keypress", '.acf-oembed [data-name="search-input"]', function (e) {
        13 == e.which && e.preventDefault()
    }), e(document).on("keyup", '.acf-oembed [data-name="search-input"]', function (t) {
        e(this).val() && t.which && acf.fields.oembed.search(e(this).closest(".acf-oembed"))
    }), e(document).on("blur", '.acf-oembed [data-name="search-input"]', function (t) {
        acf.fields.oembed.blur(e(this).closest(".acf-oembed"))
    })
}(jQuery), function (e) {
    acf.fields.radio = acf.field.extend({
        type: "radio",
        $selected: null,
        $other: null,
        actions: {ready: "render", append: "render"},
        events: {'change input[type="radio"]': "render"},
        focus: function () {
            this.$selected = this.$field.find('input[type="radio"]:checked'), this.$other = this.$field.find('input[type="text"]')
        },
        render: function () {
            "other" === this.$selected.val() ? this.$other.removeAttr("disabled").attr("name", this.$selected.attr("name")) : this.$other.attr("disabled", "disabled").attr("name", "")
        }
    })
}(jQuery), function (e) {
    acf.fields.relationship = acf.field.extend({
        type: "relationship",
        $el: null,
        $input: null,
        $filters: null,
        $choices: null,
        $values: null,
        actions: {ready: "initialize", append: "initialize"},
        events: {
            "keypress [data-filter]": "submit_filter",
            "change [data-filter]": "change_filter",
            "keyup [data-filter]": "change_filter",
            "click .choices .acf-rel-item": "add_item",
            'click [data-name="remove_item"]': "remove_item"
        },
        focus: function () {
            this.$el = this.$field.find(".acf-relationship"), this.$input = this.$el.find(".acf-hidden input"), this.$choices = this.$el.find(".choices"), this.$values = this.$el.find(".values"), this.settings = acf.get_data(this.$el)
        },
        initialize: function () {
            var t = this, i = this.$field, a = this.$el, n = this.$input;
            this.$values.children(".list").sortable({
                items: "li",
                forceHelperSize: !0,
                forcePlaceholderSize: !0,
                scroll: !0,
                update: function () {
                    n.trigger("change")
                }
            }), this.$choices.children(".list").scrollTop(0).on("scroll", function (n) {
                if (!a.hasClass("is-loading") && !a.hasClass("is-empty") && e(this).scrollTop() + e(this).innerHeight() >= e(this).get(0).scrollHeight) {
                    var s = parseInt(a.attr("data-paged"));
                    a.attr("data-paged", s + 1), t.doFocus(i), t.fetch()
                }
            }), this.fetch()
        },
        fetch: function () {
            var t = this, i = this.$field;
            this.$el.addClass("is-loading");
            var a = acf.prepare_for_ajax({
                action: "acf/fields/relationship/query",
                field_key: acf.get_field_key(i),
                post_id: acf.get("post_id")
            });
            e.extend(a, acf.get_data(this.$el)), 1 == a.paged && this.$choices.children(".list").html(""), this.$choices.children(".list").append("<p>" + acf._e("relationship", "loading") + "...</p>"), this.$el.data("xhr") && this.$el.data("xhr").abort();
            var n = e.ajax({
                url: acf.get("ajaxurl"), dataType: "json", type: "post", data: a, success: function (e) {
                    t.doFocus(i), t.render(e)
                }
            });
            this.$el.data("xhr", n)
        },
        render: function (t) {
            if (this.$el.removeClass("is-loading is-empty"), this.$choices.children(".list").children("p").remove(), !t || !t.length)return this.$el.addClass("is-empty"), void(1 == this.settings.paged && this.$choices.children(".list").append("<p>" + acf._e("relationship", "empty") + "</p>"));
            var i = e(this.walker(t));
            if (this.$values.find(".acf-rel-item").each(function () {
                    var t = e(this).attr("data-id");
                    i.find('.acf-rel-item[data-id="' + t + '"]').addClass("disabled")
                }), this.settings.s) {
                var a = this.settings.s;
                i.find(".acf-rel-item").each(function () {
                    var t = e(this).text(), i = t.replace(new RegExp("(" + a + ")", "gi"), "<b>$1</b>");
                    e(this).html(e(this).html().replace(t, i))
                })
            }
            this.$choices.children(".list").append(i);
            var n = "", s = null;
            this.$choices.find(".acf-rel-label").each(function () {
                return e(this).text() == n ? (s.append(e(this).siblings("ul").html()), void e(this).parent().remove()) : (n = e(this).text(), void(s = e(this).siblings("ul")))
            })
        },
        walker: function (t) {
            var i = "";
            if (e.isArray(t))for (var a in t)i += this.walker(t[a]); else e.isPlainObject(t) && (void 0 !== t.children ? (i += '<li><span class="acf-rel-label">' + t.text + '</span><ul class="acf-bl">', i += this.walker(t.children), i += "</ul></li>") : i += '<li><span class="acf-rel-item" data-id="' + t.id + '">' + t.text + "</span></li>");
            return i
        },
        submit_filter: function (e) {
            13 == e.which && e.preventDefault()
        },
        change_filter: function (e) {
            var t = e.$el.val(), i = e.$el.attr("data-filter");
            this.$el.attr("data-" + i) != t && (this.$el.attr("data-" + i, t), this.$el.attr("data-paged", 1), this.fetch())
        },
        add_item: function (e) {
            if (this.settings.max > 0 && this.$values.find(".acf-rel-item").length >= this.settings.max)return void alert(acf._e("relationship", "max").replace("{max}", this.settings.max));
            if (e.$el.hasClass("disabled"))return !1;
            e.$el.addClass("disabled");
            var t = ["<li>", '<input type="hidden" name="' + this.$input.attr("name") + '[]" value="' + e.$el.attr("data-id") + '" />', '<span data-id="' + e.$el.attr("data-id") + '" class="acf-rel-item">' + e.$el.html(), '<a href="#" class="acf-icon small dark" data-name="remove_item"><i class="acf-sprite-remove"></i></a>', "</span>", "</li>"].join("");
            this.$values.children(".list").append(t), this.$input.trigger("change"), acf.validation.remove_error(this.$field)
        },
        remove_item: function (e) {
            var t = e.$el.parent(), i = t.attr("data-id");
            t.parent("li").remove(), this.$choices.find('.acf-rel-item[data-id="' + i + '"]').removeClass("disabled"), this.$input.trigger("change")
        }
    })
}(jQuery), function (e) {
    function t(t, i) {
        i = e.extend({allow_null: !1, placeholder: "", multiple: !1, ajax: !1, action: "", pagination: !1}, i);
        var a = t.siblings("input"), n = {
            width: "100%",
            allowClear: i.allow_null,
            placeholder: i.placeholder,
            multiple: i.multiple,
            data: [],
            escapeMarkup: function (e) {
                return e
            }
        };
        i.multiple && (n.formatSelection = function (e, i) {
            return i.parent().append('<input type="hidden" class="acf-select2-multi-choice" name="' + t.attr("name") + '" value="' + e.id + '" />'), e.text
        }), i.allow_null && (n.placeholder = i.placeholder, t.find('option[value=""]').remove());
        var s = a.val().split(","), o = [], r = {};
        t.find("option").each(function (t) {
            var i = "_root";
            e(this).parent().is("optgroup") && (i = e(this).parent().attr("label")), r[i] || (r[i] = []), r[i].push({
                id: e(this).attr("value"),
                text: e(this).text()
            })
        }), e.each(r, function (t, i) {
            "_root" == t ? e.each(i, function (e, t) {
                n.data.push(t)
            }) : n.data.push({text: t, children: i})
        }), e.each(s, function (t, i) {
            e.each(n.data, function (e, t) {
                i == t.id && o.push(t)
            })
        }), i.ajax && (n.ajax = {
            url: acf.get("ajaxurl"),
            dataType: "json",
            type: "post",
            cache: !1,
            data: function (e, t) {
                var a = {
                    action: i.action,
                    field_key: i.key,
                    nonce: acf.get("nonce"),
                    post_id: acf.get("post_id"),
                    s: e,
                    paged: t
                };
                return a
            },
            results: function (e, t) {
                return {results: e}
            }
        }, i.pagination && (n.ajax.results = function (t, i) {
            var a = 0;
            return e.each(t, function (e, t) {
                l = 1, "undefined" != typeof t.children && (l = t.children.length), a += l
            }), {results: t, more: a >= 20}
        }, a.on("select2-loaded", function (t) {
            var i = "", a = null;
            e("#select2-drop .select2-results > li > .select2-result-label").each(function () {
                return e(this).text() == i ? (a.append(e(this).siblings("ul").children()), void e(this).parent().remove()) : (i = e(this).text(), void(a = e(this).siblings("ul")))
            })
        })), n.initSelection = function (e, t) {
            i.multiple || (o = o[0]), t(o)
        }), n.dropdownCss = {"z-index": "999999999"}, n = acf.apply_filters("select2_args", n, t, i), a.select2(n), a.select2("container").before(a), i.multiple && a.select2("container").find("ul.select2-choices").sortable({
            start: function () {
                a.select2("onSortStart")
            }, update: function () {
                a.select2("onSortEnd")
            }
        }), t.attr("disabled", "disabled").addClass("acf-disabled")
    }

    function i(e) {
        e.siblings(".select2-container").remove()
    }

    acf.fields.select = acf.field.extend({
        type: "select",
        $select: null,
        settings: {action: "", pagination: !1},
        actions: {ready: "render", append: "render", remove: "remove"},
        focus: function () {
            this.$select = this.$field.find("select"), this.$select.exists() && (e.extend(this.settings, acf.get_data(this.$select)), this.settings.action = "acf/fields/" + this.type + "/query")
        },
        render: function () {
            return this.$select.exists() && this.settings.ui ? void t(this.$select, this.settings) : !1
        },
        remove: function () {
            return this.$select.exists() && this.settings.ui ? void i(this.$select) : !1
        }
    }), acf.fields.taxonomy = acf.fields.select.extend({type: "taxonomy"}), acf.fields.user = acf.fields.select.extend({type: "user"}), acf.fields.post_object = acf.fields.select.extend({
        type: "post_object",
        settings: {pagination: !0}
    }), acf.fields.page_link = acf.fields.post_object.extend({type: "page_link"})
}(jQuery), function (e) {
    acf.fields.tab = acf.field.extend({
        type: "tab",
        actions: {ready: "initialize", append: "initialize", hide: "hide", show: "show"},
        initialize: function () {
            this.$field.siblings(".acf-tab-wrap").exists() || this.add_group(), this.add_tab()
        },
        add_tab: function () {
            var e = this.$field.find(".acf-tab"), t = this.$field.siblings(".acf-tab-wrap"), i = this.settings.key, a = ["<li>", '<a class="acf-tab-button" href="#" data-key="' + i + '">' + e.text() + "</a>", "</li>"].join("");
            t.find("ul").append(a), 1 == t.find("li").length ? (t.find("li").addClass("active"), this.show_tab_fields(this.$field)) : this.hide_tab_fields(this.$field)
        },
        add_group: function () {
            var e = this.$field.parent(), t = "";
            t = e.is("tbody") ? '<tr class="acf-tab-wrap"><td colspan="2"><ul class="acf-hl acf-tab-group"></ul></td></tr>' : '<div class="acf-tab-wrap"><ul class="acf-hl acf-tab-group"></ul></div>', this.$field.before(t)
        },
        toggle: function (t) {
            var i = this, a = t.closest(".acf-tab-wrap");
            t.parent().addClass("active").siblings().removeClass("active"), a.siblings('.acf-field[data-type="tab"]').each(function () {
                return e(this).attr("data-key") === t.attr("data-key") ? void i.show_tab_fields(e(this)) : e(this).hasClass("hidden-by-tab") ? void 0 : void i.hide_tab_fields(e(this))
            })
        },
        show_tab_fields: function (t) {
            t.removeClass("hidden-by-tab"), t.nextUntil('.acf-field[data-type="tab"]', ".acf-field").each(function () {
                e(this).removeClass("hidden-by-tab"), acf.do_action("show_field", e(this))
            })
        },
        hide_tab_fields: function (t) {
            t.addClass("hidden-by-tab"), t.nextUntil('.acf-field[data-type="tab"]', ".acf-field").each(function () {
                e(this).addClass("hidden-by-tab"), acf.do_action("hide_field", e(this))
            })
        },
        hide: function (t, i) {
            var a = t.siblings(".acf-tab-wrap").find('a[data-key="' + this.settings.key + '"]'), n = a.parent();
            return "conditional_logic" == i && t.nextUntil('.acf-field[data-type="tab"]', ".acf-field").each(function () {
                acf.conditional_logic.hide_field(e(this))
            }), n.is(":hidden") || (n.hide(), n.siblings(".active").exists()) ? void 0 : n.siblings(":visible").exists() ? void n.siblings(":visible").first().children("a").trigger("click") : void acf.fields.tab.hide_tab_fields(t)
        },
        show: function (t, i) {
            var a = t.siblings(".acf-tab-wrap").find('a[data-key="' + this.settings.key + '"]'), n = a.parent();
            "conditional_logic" == i && t.nextUntil('.acf-field[data-type="tab"]', ".acf-field").each(function () {
                acf.conditional_logic.show_field(e(this))
            }), n.is(":visible") || (n.show(), n.hasClass("active") || n.siblings(":visible").exists() || a.trigger("click"))
        }
    }), e(document).on("click", ".acf-tab-button", function (t) {
        t.preventDefault(), acf.fields.tab.toggle(e(this)), e(this).trigger("blur")
    }), acf.add_filter("validation_complete", function (t, i) {
        return e.each(t.errors, function (e, t) {
            var a = i.find('[name="' + t.input + '"]').first(), n = acf.get_field_wrap(a), s = n.prevAll('.acf-field[data-type="tab"]:first');
            if (s.exists())return n.hasClass("hidden-by-tab") ? (s.siblings(".acf-tab-wrap").find('a[data-key="' + acf.get_data(s, "key") + '"]').trigger("click"), !1) : !1
        }), t
    })
}(jQuery), function (e) {
    acf.fields.url = acf.field.extend({
        type: "url",
        $input: null,
        actions: {ready: "render", append: "render"},
        events: {'keyup input[type="url"]': "render"},
        focus: function () {
            this.$input = this.$field.find('input[type="url"]')
        },
        render: function () {
            this.$input.parent().removeClass("valid"), "http" === this.$input.val().substr(0, 4) && this.$input.parent().addClass("valid")
        }
    })
}(jQuery), function (e) {
    acf.validation = acf.model.extend({
        actions: {"ready 20": "onReady"},
        active: 1,
        ignore: 0,
        error_class: "acf-error",
        message_class: "acf-error-message",
        $trigger: null,
        onReady: function () {
            this.active = acf.get("validation"), this.active && this.add_events()
        },
        add_error: function (e, t) {
            e.addClass(this.error_class), void 0 !== t && (e.children(".acf-input").children("." + this.message_class).remove(), e.children(".acf-input").prepend('<div class="' + this.message_class + '"><p>' + t + "</p></div>")), acf.do_action("add_field_error", e)
        },
        remove_error: function (e) {
            $message = e.children(".acf-input").children("." + this.message_class), e.removeClass(this.error_class), setTimeout(function () {
                acf.remove_el($message)
            }, 250), acf.do_action("remove_field_error", e)
        },
        add_warning: function (e, t) {
            this.add_error(e, t), setTimeout(function () {
                acf.validation.remove_error(e)
            }, 1e3)
        },
        fetch: function (t) {
            var i = this, a = acf.serialize_form(t, "acf");
            a.action = "acf/validate_save_post", e.ajax({
                url: acf.get("ajaxurl"),
                data: a,
                type: "post",
                dataType: "json",
                success: function (e) {
                    i.complete(t, e)
                }
            })
        },
        complete: function (t, i) {
            i = acf.apply_filters("validation_complete", i, t);
            var a = this;
            if (t.children("." + this.message_class).remove(), e("#submitdiv").exists() && (e("#submitdiv").find(".disabled").removeClass("disabled"), e("#submitdiv").find(".button-disabled").removeClass("button-disabled"), e("#submitdiv").find(".button-primary-disabled").removeClass("button-primary-disabled"), e("#submitdiv .spinner").hide()), !i || "undefined" == typeof i.result || 1 == i.result)return t.find(".acf-postbox.acf-hidden").remove(), this.ignore = 1, acf.do_action("submit", t), void(this.$trigger ? this.$trigger.click() : t.submit());
            if (t.prepend('<div class="' + this.message_class + '"><p>' + i.message + "</p></div>"), i.errors)for (var n in i.errors) {
                var s = i.errors[n], o = t.find('[name="' + s.input + '"]').first();
                o.exists() || (o = t.find('[name^="' + s.input + '"]').first());
                var l = acf.get_field_wrap(o);
                a.add_error(l, s.message)
            }
        },
        add_events: function () {
            var t = this;
            e(document).on("focus click change", '.acf-field[data-required="1"] input, .acf-field[data-required="1"] textarea, .acf-field[data-required="1"] select', function (i) {
                t.remove_error(e(this).closest(".acf-field"))
            }), e("#save-post").exists() && e("#save-post").on("click", function () {
                t.ignore = 1, t.$trigger = e(this)
            }), e("#post-preview").exists() && e("#post-preview").on("click", function () {
                t.ignore = 1, t.$trigger = e(this)
            }), e("#submit").exists() && e("#submit").on("click", function () {
                t.$trigger = e(this)
            }), e("#publish").exists() && e("#publish").on("click", function () {
                t.$trigger = e(this)
            }), e(document).on("submit", "form", function (i) {
                return e(this).find("#acf-form-data").exists() ? (t.ignore = acf.apply_filters("ignore_validation", t.ignore, t.$trigger, e(this)), 1 == t.ignore ? (t.ignore = 0, !0) : 0 == t.active ? !0 : (i.preventDefault(), void t.fetch(e(this)))) : !0
            })
        }
    })
}(jQuery), function (e) {
    acf.fields.wysiwyg = acf.field.extend({
        type: "wysiwyg",
        $el: null,
        $textarea: null,
        toolbars: {},
        actions: {
            ready: "initialize",
            append: "initialize",
            remove: "disable",
            sortstart: "disable",
            sortstop: "enable"
        },
        focus: function () {
            this.$el = this.$field.find(".wp-editor-wrap").last(), this.$textarea = this.$el.find("textarea"), this.settings = acf.get_data(this.$el), this.settings.id = this.$textarea.attr("id")
        },
        initialize: function () {
            if ("undefined" == typeof tinyMCEPreInit)return !1;
            var e = this.get_mceInit(), t = this.get_qtInit();
            if (tinyMCEPreInit.mceInit[e.id] = e, tinyMCEPreInit.qtInit[t.id] = t, this.$el.hasClass("tmce-active"))try {
                tinymce.init(e)
            } catch (i) {
            }
            try {
                var a = quicktags(t);
                this._buttonsInit(a)
            } catch (i) {
            }
        },
        get_mceInit: function () {
            var t = this.$field, i = this.get_toolbar(this.settings.toolbar), a = e.extend({}, tinyMCEPreInit.mceInit.acf_content);
            if (a.selector = "#" + this.settings.id, a.id = this.settings.id, a.elements = this.settings.id, i)for (var n = tinymce.majorVersion < 4 ? "theme_advanced_buttons" : "toolbar", s = 1; 5 > s; s++)a[n + s] = acf.isset(i, s) ? i[s] : "";
            return a.setup = tinymce.majorVersion < 4 ? function (i) {
                i.onInit.add(function (i, a) {
                    e(i.getBody()).on("focus", function () {
                        acf.validation.remove_error(t)
                    }), e(i.getBody()).on("blur", function () {
                        i.save(), t.find("textarea").trigger("change")
                    })
                })
            } : function (e) {
                e.on("focus", function (e) {
                    acf.validation.remove_error(t)
                }), e.on("blur", function (i) {
                    e.save(), t.find("textarea").trigger("change")
                })
            }, a = acf.apply_filters("wysiwyg_tinymce_settings", a, a.id)
        },
        get_qtInit: function () {
            var t = e.extend({}, tinyMCEPreInit.qtInit.acf_content);
            return t.id = this.settings.id, t = acf.apply_filters("wysiwyg_quicktags_settings", t, t.id)
        },
        disable: function () {
            try {
                var e = tinyMCE.get(this.settings.id), t = tinyMCE.DOM.get(this.settings.id);
                val = t.value, e.destroy(), this.$field.find(".wp-editor-wrap").hasClass("html-active") && (t.value = val)
            } catch (i) {
            }
        },
        enable: function () {
            if (!this.$field.find(".wp-editor-wrap").hasClass("html-active"))try {
                tinyMCE.init(tinyMCEPreInit.mceInit[this.settings.id])
            } catch (e) {
            }
        },
        get_toolbar: function (e) {
            return "undefined" != typeof this.toolbars[e] ? this.toolbars[e] : !1
        },
        _buttonsInit: function (e) {
            var t = ",strong,em,link,block,del,ins,img,ul,ol,li,code,more,close,";
            canvas = e.canvas, name = e.name, settings = e.settings, html = "", theButtons = {}, use = "", settings.buttons && (use = "," + settings.buttons + ",");
            for (i in edButtons)edButtons[i] && (id = edButtons[i].id, use && -1 !== t.indexOf("," + id + ",") && -1 === use.indexOf("," + id + ",") || edButtons[i].instance && edButtons[i].instance !== inst || (theButtons[id] = edButtons[i], edButtons[i].html && (html += edButtons[i].html(name + "_"))));
            use && -1 !== use.indexOf(",fullscreen,") && (theButtons.fullscreen = new qt.FullscreenButton, html += theButtons.fullscreen.html(name + "_")), "rtl" === document.getElementsByTagName("html")[0].dir && (theButtons.textdirection = new qt.TextDirectionButton, html += theButtons.textdirection.html(name + "_")), e.toolbar.innerHTML = html, e.theButtons = theButtons
        }
    }), e(document).ready(function () {
        e("#wp-acf_content-wrap").exists() && e("#wp-acf_content-wrap").parent().appendTo("body")
    })
}(jQuery);
